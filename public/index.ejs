<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="environment" content="<%-envType%>">
    <link  rel="shortcut icon" type="image/x-icon" href="/public/favicon.ico"/>
    <title>Iso</title>
    <link rel="stylesheet" href="/public/fonts/fonts.css<%-version%>">
    <link rel="stylesheet" href="/bundle.css<%-version%>" >
    <%-styles.map(({file, version}) => {
        return `<link rel="stylesheet" href="/${file}${version}"/>`
    }).join("\n")%>
    <script async src="https://api-maps.yandex.ru/2.1/?lang=ru_RU"></script>
</head>
<body>
    <div id="root"><%-html%></div>
    <script>window.__initialData__ = <%-initialData%></script>
    <script defer src="/bundle.js<%-version%>"></script>
    <%-scripts.map(({file, version}) => {
        return `<script defer src="/${file}${version}"></script>`
    }).join("\n")%>
    <script defer src="/public/main.js"></script>
</body>
</html>
